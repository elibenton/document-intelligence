[project]
name = "document-intelligence-ml"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "chandra-ocr",
    "gliner",
    "rapidfuzz",
    "httpx",
    "Pillow",
    "modal",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
