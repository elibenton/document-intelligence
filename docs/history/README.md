# Historical plans

These plans shipped and are kept for the reasoning, not as a map of the code.
File paths and line anchors refer to the tree as it was then — `processingNode.ts`,
`searchNode.ts`, `pageImages.ts`, `App.tsx`, the workpool components, and the
server render pipeline have all since been removed. Do not navigate from these
documents; grep the current tree.
